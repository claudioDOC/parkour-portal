node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Database
/data/*.db
/data/*.db-shm
/data/*.db-wal
/data/*.backup*

# Uploads
/static/uploads/
/data/uploads/

# Cursor / lokale KI-Regeln (nicht ins öffentliche Repo)
.cursor/

# Lokale Backups (enthalten DB und .env-Kopien)
/backups

# Lokale Android-Toolchain (SDK + Gradle, ~600 MB)
/tools
android/.gradle/
android/app/build/
android/build/

# Produktivdaten (SQLite + Uploads) — nie ins Repo
/data

# Gradle-/Android-Caches vom lokalen APK-Build (Home = Repo-Root)
/.android
/.gradle

# Abnahme-Umgebung (git worktree + eigene DB)
/staging
/staging-data

# Agent-/Shell-Dateien — nie ins Repo
/.claude
/.claude.json
/.bashrc
/.bash_history
/.config
/.cache
/.local
/.npm
/.android
/.gradle
/tools

# Native App (React Native / Expo)
/native/node_modules
/native/dist
/native/.expo
/native/android/build
/native/android/app/build
/native/android/.gradle
/native/android/.kotlin
/native/android/local.properties

# Home-Verzeichnis-Dateien des Server-Benutzers — niemals ins Repo
.bash_history
.bashrc
.cache/
.claude/
.claude.json
.config/
.expo/
.local/
.npm/
.npmrc
